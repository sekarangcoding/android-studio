<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.perpustakaan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".details"></activity>
        <activity
            android:name=".Password"
            android:theme="@style/AppTheme.Password" />
        <activity android:name=".Profile" />
        <activity android:name=".Jurnal_Detail" />
        <activity android:name=".Skripsi_Detail" />
        <activity android:name=".DetailBuku" />
        <activity android:name=".Jurnal" />
        <activity android:name=".Skripsi" />
        <activity android:name=".Buku" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>